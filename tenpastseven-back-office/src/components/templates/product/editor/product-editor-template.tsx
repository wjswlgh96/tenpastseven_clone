import ProductEditorFormSection from "@/components/organisms/product/editor/product-editor-form-section";

export default function ProductEditorTemplate({ id }: { id?: string }) {
  return <ProductEditorFormSection id={id} />;
}
